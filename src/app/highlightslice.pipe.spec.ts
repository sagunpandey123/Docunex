import { HighlightslicePipe } from './highlightslice.pipe';

describe('HighlightslicePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightslicePipe();
    expect(pipe).toBeTruthy();
  });
});
