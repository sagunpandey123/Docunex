import { AmpersandEncodePipe } from './ampersand-encode.pipe';

describe('AmpersandEncodePipe', () => {
  it('create an instance', () => {
    const pipe = new AmpersandEncodePipe();
    expect(pipe).toBeTruthy();
  });
});
