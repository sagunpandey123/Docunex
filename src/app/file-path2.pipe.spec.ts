import { FilePath2Pipe } from './file-path2.pipe';

describe('FilePath2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilePath2Pipe();
    expect(pipe).toBeTruthy();
  });
});
